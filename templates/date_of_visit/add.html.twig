{% extends 'base.html.twig' %}

{% block title %}Dodaj Wizytę{% endblock %}

{% block body %}
    <div class="container w-25">
        <div class="container form" style="margin-top:150px;">
            <h1>Dodaj terminy wizyt</h1>
            {{ form_start(form) }}
            {{ form_row(form.Doctor) }}
            {{ form_row(form.date) }}
            {{ form_row(form.startTime) }}
            {{ form_row(form.endTime) }}
            <button type="submit" class="btn btn-green mt-2 mb-4">Dodaj Wizyty</button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
