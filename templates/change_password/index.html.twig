{% extends 'base.html.twig' %}

{% block title %}Hello ChangePasswordController!{% endblock %}

{% block body %}

    <div class="container-sm w-25">
        <h1>Zmiana hasła</h1>
        {{ form_start(form) }}
        {{ form_row(form.plainPassword) }}
        <button type="submit" class="btn btn-dark mt-2">Zmień hasło</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
