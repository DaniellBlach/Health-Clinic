{% extends 'base.html.twig' %}

{% block title %}Wyniki badań{% endblock %}

{% block body %}
    <div class="container w-25">
        <h1 class="text-center">Badania krwi</h1>
        <table class="table table-dark">
            <thead>
            <tr>
                <th scope="col">Dzień</th>
                <th scope="col">Godzina pobrania</th>
                <th scope="col"></th>
            </tr>
            </thead>
            <tbody>
            {% for test in tests %}
                <tr>
                    <td>{{ test.date|date("d/m/Y") }}</td>
                    <td>{{ test.time|date("H:i") }}</td>
                    <td><a href="">
                            <button class="btn btn-danger">Zobacz szczegóły</button>
                        </a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
